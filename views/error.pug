extends layout.pug

block content 
  include includes/breadcrumb.pug

  article.grid-container
    .error-message
      h2 Error #{err.status}: #{err.message}

      h4 Here's the stack trace in case you want to know more.
      
      .error-stack= err.stack

  include includes/breadcrumb.pug